# Simulation Baseline

## Requirements
- Python 3.9+
- numpy, matplotlib, pytest
- sam3 access

## Run the simulation
H400 or above once setup run:
cd /workspace/adembeddings/ad_pipeline
source .venv/bin/activate
streamlit run app.py --server.port 8501 --server.address 0.0.0.0
